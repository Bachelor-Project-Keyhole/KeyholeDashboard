import * as React from 'react'
import MenuLayout from '../../components/MenuLayout'

const Menu = ({ ...props }: any) => {
  const a = 1
  return (
        <MenuLayout>
            <div>Menu</div>
        </MenuLayout>
  )
}

export default Menu
